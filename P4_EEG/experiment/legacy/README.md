# legacy/

本目录存放 **已停用 / 仅供参考** 的旧实验脚本，不再被当前 Python 流程调用。

| 文件 | 来源 | 说明 |
|:---|:---|:---|
| `task3_v4.m` | MATLAB / Psychtoolbox | AAD（注意力解码）实验旧版本脚本，含 GUI 启动对话框、音频播放列表、Trigger 串口逻辑。当前 P4 已用 Python + PsychoPy 重写，此脚本仅作为 Marker / 流程对照参考保留。 |

## 注意事项

- 这些文件不在 `python launcher.py` 的调用路径上，删除不会影响 S1–S4 任何 Session。
- 如需查阅旧 Marker 编码或 AAD 流程，可直接打开 `.m` 文件查看，但不要尝试在当前环境直接运行（依赖与硬件路径都已变更）。
- 新增任何"参考性、不参与运行"的脚本，请同样放进本目录，并在上表中登记。
